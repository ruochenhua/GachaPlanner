/* 概率区间展示组件样式 */

.probability-interval {
  width: 100%;
  padding: 16px;
  background-color: var(--color-bg-page);  /* Gray 50 */
  border-radius: 8px;
  box-sizing: border-box;
}

/* 空数据状态 */
.empty-state {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
}

.empty-text {
  font-size: 14px;
  color: var(--color-text-secondary);  /* Gray 500 */
}

/* 区间标签 */
.interval-label {
  display: flex;
  align-items: center;
  margin-bottom: 12px;
}

.label-text {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-text-primary);
}

.confidence-level {
  font-size: 12px;
  color: var(--color-text-secondary);  /* Gray 500 */
  margin-left: 4px;
}

/* 区间可视化 */
.interval-visual {
  position: relative;
  height: 50px;
  margin-bottom: 8px;
}

/* 区间轨道 */
.interval-track {
  position: absolute;
  top: 10px;
  left: 0;
  right: 0;
  height: 8px;
  background-color: var(--color-border-divider);
  border-radius: 4px;
}

/* 区间范围条 */
.interval-range {
  position: absolute;
  top: 0;
  height: 100%;
  background-color: var(--color-primary);  /* Primary 奶茶棕 */
  border-radius: 4px;
  opacity: 0.6;
}

/* 当前概率标记点 */
.current-marker {
  position: absolute;
  top: 50%;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  transform: translateY(-50%);
  border: 2px solid var(--color-bg-card);
  box-shadow: 0 1px 3px var(--color-shadow);
  z-index: 2;
}

.current-marker.green {
  background-color: var(--color-success);  /* Success Green */
}

.current-marker.yellow {
  background-color: var(--color-warning);  /* Warning Yellow */
}

.current-marker.red {
  background-color: var(--color-danger);  /* Danger Red */
}

/* 区间端点数值 */
.interval-values {
  position: absolute;
  top: 24px;
  left: 0;
  right: 0;
}

.lower-value,
.upper-value {
  position: absolute;
  font-size: 11px;
  color: var(--color-text-secondary);  /* Gray 600 */
  transform: translateX(-50%);
  white-space: nowrap;
}

/* 不确定性警告 */
.uncertainty-hint {
  margin-top: 8px;
  padding: 8px 12px;
  background-color: var(--color-state-warning-bg);
  border-radius: 4px;
}

.hint-text {
  font-size: 12px;
  color: var(--color-primary-dark);
}

/* 区间摘要（可访问性） */
.interval-summary {
  margin-top: 8px;
  text-align: center;
  opacity: 0;
  height: 0;
  overflow: hidden;
}

.summary-text {
  font-size: 12px;
  color: var(--color-text-secondary);
}

/* 响应式适配 */
@media screen and (max-width: 320px) {
  .probability-interval {
    padding: 12px;
  }

  .label-text {
    font-size: 13px;
  }

  .lower-value,
  .upper-value {
    font-size: 10px;
  }
}
/* ============================================================
   ⚠️ 已废弃（Deprecated）
   ============================================================
   该文件内容已合并至 app.wxss，统一使用 --color-* 前缀的变量体系。
   请勿在新代码中引用此文件，后续版本将彻底移除。
   
   迁移指南：
   --gray-50    →  --color-gray-50
   --primary    →  --color-primary
   --success    →  --color-success
   --warning    →  --color-warning
   --error      →  --color-danger
   --font-size-body  →  --font-size-base
   ============================================================ */
